apiVersion: v1
kind: Service
metadata:
  name: {{ include "nodejs-auth-app.fullname" . }}
  labels:
    {{- include "nodejs-auth-app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "nodejs-auth-app.selectorLabels" . | nindent 4 }}
